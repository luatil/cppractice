#include <bits/stdc++.h>
using namespace std;

#define ll long long

const int N = 2*(1e5) + 434;
int a[N];
int b[N];

int main() 
{
    int n, m, k; cin >> n >> m >> k;
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }
}