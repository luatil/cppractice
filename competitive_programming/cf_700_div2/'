#include <bits/stdc++.h>
using namespace std;
 
#define all(v) v.begin(), v.end()
#define REP(i, n) for(int i = a; i < n; i++)
#define ll long long 

template <typename T> 
void print(T v)
{
	for(const auto& el: v) cout << el << " ";
	cout << "\n";
}

const int maxn = 100'010

int arr[maxn];
// using as a table for the next position of a given element, a[3] = 4 means that 
// the last found 3 element is at index 4. This will change by iterating through it backwards.
// This is operating more like a map as an array.
int next_index_of[maxn];  
// Using as a index map, so for instance next_equal_at_index[1] for the array 1 1 2 2 3 3 3
//
int next_equal_at_index[max];

int main()
{
	int n; cin >> n;
	for(int i = 1; i <= n; ++i)
		cin >> arr[i];
}
