#include <bits/stdc++.h>
using namespace std;

#define all(v) v.begin(), v.end()
#define REP(i, n) for(int i = 0; i < n; i++)
#define ll long long

void solve()
{
    int n; cin >> n;
    vector<int> tags(n+1);
    vector<int> scores(n+1);
    for(int i = 1; i <= n; i++) cin >> tags[i];
    for(int i = 1; i <= n; i++) cin >> scores[i];



}

void test()
{

}

void drive()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t; cin >> t;
    while(t--)
        solve();
}

int main()
{
    drive();
}
